<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StellarDemo">

        <provider
            android:name="roro.stellar.StellarProvider"
            android:authorities="${applicationId}.stellar"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".FollowStellarStartup"
            android:enabled="true"
            android:exported="true" >
            <intent-filter>
                <action android:name="roro.stellar.intent.action.FOLLOW_STELLAR_STARTUP" />
            </intent-filter>
            <intent-filter>
                <action android:name="roro.stellar.intent.action.FOLLOW_STELLAR_STARTUP_ON_BOOT" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="roro.stellar.permissions"
            android:value="stellar,follow_stellar_startup,follow_stellar_startup_on_boot" />
    </application>

</manifest>

